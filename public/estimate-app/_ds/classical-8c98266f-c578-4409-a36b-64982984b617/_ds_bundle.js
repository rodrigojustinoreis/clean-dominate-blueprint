/* Design-system component bundle placeholder.
   This prototype renders entirely from inline markup plus the <ccc-house>
   custom element (ccc-house.js); it does not mount any <dc-import> design-
   system components, so no bundle code is required at runtime. Kept as an
   empty module so the <script src> reference resolves cleanly. */
